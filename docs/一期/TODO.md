# 一期待确认事项与技术债

> 本文件只记录尚未确认或尚未处理的事项，不代表相关方案已经决定。处理前应结合 `设计.md`、拆分计划和当前实现逐项确认。

## 恢复开发前

- [ ] 保护当前未提交的分类 CRUD 和房间校验改动，不直接清理或重置工作区。
- [ ] 复查暂存区状态：`api/asset/v1/category.go` 和 `internal/logic/asset/category.go` 当前为先暂存骨架、后继续修改的 `AM` 状态。
- [ ] MySQL 可用后重新检查数据库连接、表结构和已有数据，再执行接口写入验证。

## 当前功能缺口

- [ ] 实现分类 Update Controller，移除 `CodeNotImplemented` 返回。
- [ ] 实现分类 View 和 List Logic，避免空命名返回值在未实现时仍通过编译。
- [ ] 完善分类约束：父分类不能选择自己、父分类存在性、是否限制循环层级。
- [ ] 分类删除时增加产品引用保护，并验证子分类引用保护。
- [ ] 资产功能完成后，为房间删除补齐资产引用保护并执行实际接口验证。
- [ ] 实现产品 CRUD。
- [ ] 实现资产 CRUD、自动编码、入库、转移和位置记录。
- [ ] 增加自动化测试；当前 `go test ./...` 仅执行编译检查，没有测试用例。

## 分层与接口设计

- [ ] 确认 `Code` 字段在建筑、楼层、房间、分类和产品中的实际用途及是否全部保留。
- [ ] 确认分页参数和业务参数校验的职责边界：API 校验标签、Controller 转发、Logic 业务校验。
- [ ] 确认 Controller 是否继续保持 GoFrame 生成的“一接口一文件”结构，不在一期中贸然合并文件。
- [ ] 确认详情和列表返回值是否需要在 ID 之外附加关联名称。
- [ ] 确认 API 层嵌入 `internal/model` 的现有方案：一期保持一致，还是在产品和资产实现前调整为 API 模型与内部业务模型分离。

## 数据库约束

- [ ] 评估并补充符合业务唯一性的联合唯一索引，例如同一建筑下 `floor_no` 唯一。
- [ ] 逐表确认唯一索引与软删除的兼容方式，避免软删除后无法重新使用业务编码。
- [ ] 仓库如需同步到远端，改用环境变量或本地覆盖配置保存数据库凭据，避免提交明文凭据。
