# AGENTS.md

## 基本要求

1. 必须使用中文进行对话、分析、汇报和编写文档。
2. 普通对话结束后必须发送：`Ciallo～(∠・ω < ) ⌒ ★`。
3. 修改代码或文档前，必须先查询是否有可使用的本地 SKILL、项目文档、知识库、已有实现或现成工具。
4. 当上下文过长、信息开始混乱或可能遗忘已确认内容时，应提醒用户：建议压缩上下文。

## 项目定位

当前项目是个人资产管理系统后端服务，项目根目录为：

```text
E:\Projects\Personal\asset_management_svr
```

一期目标是基于 GoFrame + MySQL 实现个人资产管理系统后端 API。第一期只实现后端接口，不实现前端页面。

一期文档以以下目录为准：

```text
docs\一期
```

关键文档：

- `docs\一期\设计.md`：一期设计和业务范围。
- `docs\一期\计划.md`：一期完整实施计划。
- `docs\一期\计划拆分\README.md`：按 Task 和 Step 拆分后的预览入口。
- `docs\一期\计划拆分\task-*/step-*.md`：具体执行步骤。

## 协作方式

1. 用户负责按计划执行实现。
2. Agent 默认负责验收，不主动修改业务代码。
3. 当用户说“验收 Task X”或“验收 Step X”时，应优先读取 `docs\一期\计划拆分` 下对应任务或步骤文件，再对照实际代码、配置和运行结果验收。
4. 如用户明确要求 Agent 修改、修复或生成文件，可以执行，但必须保持范围明确，不扩展到未指定内容。
5. 若发现计划与当前项目状态不一致，应先说明差异，再给出建议，不得擅自扩大实现范围。

## 信息确认原则

1. 避免仅凭推测回答或验收。
2. 回答、修改或验收前，应优先基于以下来源确认信息：
   - 当前项目文件、已有实现、配置、测试结果；
   - `docs\一期` 下的一期设计和计划文档；
   - 用户明确说明；
   - 本地 SKILL、知识库、已有工具；
   - 必要时再查询官方文档或联网资料。
3. 当需求不明确、存在多种实现方式、影响范围较大或可能破坏现有逻辑时，必须先向用户确认。
4. 当需求明确、范围清楚、风险较低时，可以直接执行，并在完成后说明处理内容和验证结果。
5. 涉及时效性信息时，例如版本、接口文档、第三方库最新用法等，应优先使用官方来源或联网查询。

## 一期业务范围

一期包含：

- 建筑、楼层、房间三层位置维度管理。
- 分类、产品、资产三层资产维度管理。
- 资产支持手动编码和自动编码。
- 资产支持入库和位置转移。
- 资产表保存当前位置。
- 位置变更写入资产位置记录表。
- 基础资料删除时做引用保护。

一期不包含：

- 前端页面。
- 登录、权限、菜单。
- 采购、领用、借用、归还。
- 盘点、维修、报废流程。
- 图片、附件、二维码。
- 多租户或多人协作。

## 技术约定

1. 后端框架：GoFrame。
2. 数据库：MySQL。
3. 接口形态：REST 风格后端 API。
4. 数据访问：优先使用 `gf gen dao` 生成 DAO、Entity、DO。
5. 事务边界：资产入库、资产转移、自动编码新增资产。
6. 生成文件如 `internal/dao`、`internal/model/do`、`internal/model/entity` 默认不手工编辑。

建议分层：

```text
api/asset/v1
internal/controller/asset
internal/service
internal/logic/asset
internal/dao
internal/model
manifest/sql
```

## 验收要求

验收时必须基于新鲜证据，不得仅凭“应该可以”判断。

常用验收动作：

1. 对照 `docs\一期\设计.md` 确认业务范围。
2. 对照 `docs\一期\计划拆分` 确认当前 Task 或 Step 的要求。
3. 检查实际文件是否按计划创建或修改。
4. 运行必要命令，例如：

```powershell
go test ./...
go build ./...
gf -v
gf gen dao
```

5. 对接口类任务，应在服务启动后使用请求验证关键场景。
6. 明确输出验收结论：通过、不通过、部分通过或无法验收。
7. 如不通过，应说明具体问题、依据、影响和建议修法。

## 汇报格式

任务完成或验收完成后，应简要说明：

- 本次检查或处理了什么；
- 是否运行校验；
- 结论是什么；
- 是否存在未处理项、风险或阻塞。

汇报应简洁、客观，不输出无关内容。

## 代码与文档修改规范

1. 修改已有文件前，应先理解当前文件结构、调用关系、命名风格和注释风格。
2. 修改已有文件时，优先保持当前结构和风格。
3. 新建文件或大范围调整时，应遵守项目现有分层和一期计划。
4. 只处理用户指定范围，未经用户同意不修改无关内容。
5. 不得随意删除已有注释，除非注释明显错误且本次修改与其直接相关。
6. 如果准备修改的代码已经被人工改动，应避免覆盖人工改动，并在最终汇报中说明。
7. 如果项目后续引入 ESLint 或其他代码检查工具，实际修改相关代码后应按项目要求运行对应校验。

## Git 约定

当前项目可能不是 Git 仓库。验收或执行计划中的“提交”步骤前，应先确认是否存在 `.git`。

如果不是 Git 仓库：

- 不应把“提交”作为必过项；
- 应在验收结论中标记该步骤不适用；
- 如用户要求使用 Git，再由用户确认是否初始化仓库。
